Data directory for the Face Recognition App.

Structure:

- `known_faces/` — подпапки с именами людей, внутри одна или несколько фотографий (`.jpg`) лица.
- `models/` — дополнительные модели, если вы решите использовать собственные сетевые веса.


